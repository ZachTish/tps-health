# 3.5.0

Minor: library-note writers consume GCM classifications; logged records retain their native writer.

GCM owns opt-in parent/subtype classifications using the existing record-kind mapping. Legacy key-only mappings and unconfigured vaults retain their behavior. Internal record kinds and IDs remain stable. Upgrading performs no note migration, cleanup, or automatic repair. The user's explicitly requested one-time vault migration is separate. Calendar public classifications remain template-owned.

Validation: 485 checks passed; one live USDA test skipped because no live test API key was supplied. TypeScript, separate production build and test-vault deployment passed. Plugins were reloaded by manifest ID. Installed synthetic creation/update checks verified food parent/subtype fields, unchanged IDs, calendar template classification and body preservation, absent unsolicited status, and finance classification round trips. Temporary settings were restored and fixtures archived. No outbound provider was called.

Minimum Obsidian: 1.12.0. Tested in Obsidian Plugin Test Vault and ready for the user's BRAT pull. Physical iPhone acceptance remains outstanding. This release does not by itself install into production. Hierarchical mappings require GCM 3.5.0 and matching domain consumers; the one-time migration must coordinate note/template/filter settings.

SHA-256:

- `main.js`: `1508146d328c0aa07b9a716a9abc3ec4448de93d1fd5043cd2cbacd4d0c766f8`
- `manifest.json`: `980854e98b25146ac038db8bc6a09b26075d3e3e3a451d9384f2261678b4b5d5`
- `styles.css`: `67890d15e253474c490d71256412db90cb8e5d60e6e644269bff27785d1b718b`
